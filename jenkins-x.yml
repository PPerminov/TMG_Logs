pipelineConfig:
  pipelines:
    pullRequest:
      build:
        preSteps:
        - command: echo pullrequest build prestep
        steps:
        - command: echo pullrequest build step
      preBuild:
        preSteps:
        - command: echo pullrequest prebuild prestep
        replace: true
        steps:
        - command: echo pullrequest prebuild step
      setup:
        steps:
        - command: echo pullrequest setup step
